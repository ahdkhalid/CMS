<%= form_for(@commenter) do |f| %>
  <% if @commenter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@commenter.errors.count, "error") %> prohibited this commenter from being saved:</h2>

      <ul>
      <% @commenter.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email_address %><br />
    <%= f.text_area :email_address %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :page_id %><br />
    <%= f.number_field :page_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
